swagger: '2.0'
info:
  title: Cloud Endpoints + Cloud Run
  description: Sample API on Cloud Endpoints with a Cloud Run backend
  version: 2.0.0
host:  proxy-691683364317.us-central1.run.app
basePath: "/v2"

schemes:
  - https
produces:
  - application/json
x-google-backend:
  address: https://pluralsight-app-691683364317.us-central1.run.app
  protocol: h2

paths:
  /cloud-endpoints:
    get:
      summary: Greet a user
      operationId: hello
      responses:
        '200':
          description: A successful response
          schema:
            type: object
            required: 
              - msg
              - ver
            properties:
              msg:
                type: string
                description: The message of the response
              ver:
                type: number
                description: The version number of the api
      